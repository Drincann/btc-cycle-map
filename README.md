# BTC Cycle Map

A static BTC halving-cycle comparison chart.

- Historical cycles are bundled in `data/historical_cycles.json`.
- The current cycle is loaded in the browser from public BTC/USDT APIs.
- No backend is required.

Open `index.html` through any static server, or publish the repository with GitHub Pages.
